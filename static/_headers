# Cloudflare Pages headers. Docusaurus fingerprints its assets, so hashed
# files under /assets can be cached aggressively and immutably.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
